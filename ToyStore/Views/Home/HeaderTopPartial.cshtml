@using SourceCode.Models
<!--header-->
<div class="header">
    <div class="header-two navbar navbar-default">
        <!--header-two-->
        <div class="container">
            <div class="nav navbar-nav navbar-left header-two-left">
                <ul style="display:flex;">
                    
                 @*   <li style="display:flex "><a class="cd-search-trigger" href="#cd-search" style="display: flex; align-items: center;"> <span></span></a> <i class="fa-solid fa-magnifying-glass"></i>Tìm kiếm</li>*@
                    <i class="fa-solid fa-magnifying-glass" style="padding-right:10px"></i> <a href="#cd-search" style="padding-right:20px">Tìm kiếm</a>
                    <li>
                        @if (Session["User"] != null && Session["User"] != "")
                        {

                            <a href="@Url.Action("SignOut","Home")">Đăng xuất </a>

                        }
                        else
                        {
                            <a @*href="/dang-ky">Đăng ký </a> hoặc <a*@ href="/dang-nhap">Đăng nhập </a>
                        }
                    </li>
                </ul>
            </div>
            <div class="nav navbar-nav logo wow zoomIn animated" data-wow-delay=".7s">
                <h1><a href="/trang-chu">OUUE SHOP</b><span class="tag"> </span> </a></h1>
            </div>
            <div class="nav navbar-nav navbar-right header-two-right">

                <div class="header-right my-account" style=" margin-left: 2em; padding-left: 1em;">
                    @if (Session["User"] != null && Session["User"] != "")
                    {
                        User user = (User)Session["User"];
                        <input type="text" id="UserID" value="@user.ID" hidden />
                        <li><a href="@Url.Action("Index", "User")">OUUE SHOP</a></li>
                    }
                </div>
                <div class="header-right my-account">
                    @if (Session["User"] != null && Session["User"] != "")
                    {
                        <a href="/quay-trung-thuong"> Săn Voucher</a>
                    }
                </div>
                <div class="header-right cart" id="DivCart">
                    @Html.Action("CartPartial", "Cart")
                </div>


                <div class="clearfix"> </div>
            </div>
            <div class="clearfix"> </div>
        </div>
    </div>
    @Html.Action("MenuPartial", "Home")
</div>
<!--//header-->


<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />